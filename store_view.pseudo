# Displays product listings

# Placeholder function:
# function display_products()


#Display each product in a proper format
FUNCTION display_products()
	products=get_all_products
	
	FOR EACH product in products
	PRINT "Product Name: " + product.name
	PRINT "Brand: " + product.brand
	PRINT "Color: " + product.color
	PRINT "Dimensions (Width x Height x Length): " TO_STRING(product.width) + "x" +TO_STRING(product.height) ++ TO_STRING(product.length)
	
	END FOR
END FUNCTION
